return {
  'akinsho/toggleterm.nvim',
  version = '*',
  opts = {
    open_mapping = [[<C-`>]],
    direction = 'float',
    close_on_exit = true, -- close the terminal window when the process exits
  },
}
